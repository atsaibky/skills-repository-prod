---
"mattpocock-skills": minor
---

Add the `writing-for-humans` skill: a model-invoked counterpart to `writing-for-agents` that strips the patterns marking prose as AI-generated. It carries a named list of nine tells, each with its fix, and runs as a pass over a finished draft rather than a constraint while writing.
